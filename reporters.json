# This file is a lie.  It does not work.
# See instead: venv/lib/python3.9/site-packages/approvaltests/reporters/reporters.json

[
  {
    "name": "genericdiff",
    "options": {
      "command": "diff -u {received} {approved}"
    }
  }
]
